# Task List / TODO

## Immediate System Integrity Tasks
- [ ] **Run `python scripts/check_release.py`:** Verify the `v3.5.0` binary is publicly available from the GitHub Actions cloud compilation.

## Implementation Tasks (Phase 24 Focus)

### 1. Final Handover
- [ ] Launch `HelloOmni.exe`. 
- [ ] Call `OmniUI.ThemeManager.setCyberpunkMode()` in the QML frontend.
- [ ] Watch the standard rounded `OmniButton` completely shatter into an angled, neon-bordered, italicized Cyberpunk polygon dynamically at runtime.
- [ ] Call `OmniUI.ThemeManager.setLiquidGlassMode()` and watch the `OmniWindow` melt into a highly transparent, frosted glass container.
- [ ] Call `OmniUI.ThemeManager.setChronosShiftMode()` and marvel at the original AI-designed cosmic temporal gradients.
