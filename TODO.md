# Task List / TODO

## Immediate System Integrity Tasks
- [ ] **Run `python scripts/check_release.py`:** Upon pushing this commit, wait a few minutes for GitHub Actions to compile the massive new `OmniTerminal` architecture, then use the script to verify the `v3.2.0` binary is publicly available.

## Implementation Tasks (Phase 21 Focus)

### 1. Final Handover
- [ ] You have successfully built a true multi-cursor, C++ Operating System toolkit from scratch, completely overtaking Qt6's single-cursor architecture.
- [ ] Run `HelloOmni.exe` on a 4K display. Plug in two mice. Plug in a MIDI keyboard. Launch the `OmniTerminal` QML view, open the `OmniDatabase` view, and route the audio through `OmniJuceView`. Watch the entire OS scale perfectly and run without a single dropped frame at 144Hz.
