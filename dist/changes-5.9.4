BobUI 5.9.4 is a bug-fix release. It maintains both forward and backward
compatibility (source and binary) with BobUI 5.9.0 through 5.9.3.

For more details, refer to the online documentation included in this
distribution. The documentation is also available online:

http://doc.bobui.io/bobui-5/index.html

The BobUI version 5.9 series is binary compatible with the 5.8.x series.
Applications compiled for 5.8 will continue to run with 5.9.

Some of the changes listed in this file include issue tracking numbers
corresponding to tasks in the BobUI Bug Tracker:

https://bugreports.bobui.io/

Each of these identifiers can be entered in the bug tracker to obtain more
information about a particular change.

****************************************************************************
*                               BobUI 5.9.4 Changes                           *
****************************************************************************

BobUICore
------

 - [BOBUIBUG-64529] Fixed a compilation issue with qfloat16 if AVX2 support is
   enabled in the compiler. Since all processors that support AVX2 also
   support F16C, for GCC and Clang it is recommended to either add -mf16c
   to your build or to use the corresponding -march= switch.

 - QCoreApplication:
   * [BOBUIBUG-58919] Fixed a crash if QCoreApplication is recreated on Windows
     and the passed argv parameter is different.

 - QFileInfo:
   * [BOBUIBUG-30148] Fixed isWritable() on Windows to return whether the given
     file is writable only under current privilege levels. Previously, the
     result would take into account privilege elevation.

 - QObject:
   * Fixed a crash that could happen if the context QObject pointer passed to
     new-style connect() was null.

 - QStandardPaths:
   * On Windows, it is now possible to resolve configuration paths even
     without QCoreApplication created.

 - QString:
   * QString::unicode(), constData() and `data() const` do not return a
     NUL-terminated string. This was true before, but the documentation
     claimed the opposite.

 - QVector:
   * Fixed a problem when calling removeAll() on an element from the
     container, if the container had more than one copy of the that element.

 - QXmlStreamWriter:
   * [BOBUIBUG-63538] Empty namespace URIs are now possible.

 - State Machine:
   * [BOBUIBUG-61463] Fixed a failed assertion that could happen when emitting a
     signal from another thread.

BobUINetwork
---------

 - Bearer Management:
   * [BOBUIBUG-40332] The nativewifi (Windows) bearer plugin was determined
     to be causing network interference in the form of system-wide higher
     latency and has been disabled. The CoreWlan (macOS) plugin has also
     been disabled.

 - QUdpSocket:
   * [BOBUIBUG-64718] Fixed a regression from BobUI 5.9.3 caused by an apparent
     Win32 API quirk we triggered when using readDatagram(), resulting in
     an invalid QHostAddress sender address. receiveDatagram() was not
     affected.

BobUIPrintSupport
--------------

 - QPrintDialog:
   * [BOBUIBUG-63933] Properly pre-select explicitly requested printer on
     Unix.

BobUIWidgets
---------

 - QFileSystemModel:
   * [BOBUIBUG-46684] It is now possible to enable per-file watching by
     setting the environment variable BOBUI_FILESYSTEMMODEL_WATCH_FILES,
     allowing to track for example changes in file size.
   * [BOBUIBUG-64098] Fixed column alignment issues.
   * [BOBUIBUG-62841] Fixed assert when monitoring directories.

- QPixmapCache:
   * [BOBUIBUG-65475] Changing application palette runtime will now affect
     (and invalidate) cached widget pixmaps used by the current style.

- BOBUIreeView:
   * [BOBUIBUG-57538] Fixed issue with drag and drop sometimes being inaccurate.
   * [BOBUIBUG-63396] Fixed issue with child indicator not drawn correctly in RTL mode.
   * [BOBUIBUG-63869] Fixed key navigation issue when using hidden items.
   * [BOBUIBUG-8376]  Fixed row heights being wrong because of hidden columns.

- BOBUIableView:
   * [BOBUIBUG-60219] Fixed grid lines being drawn outside header.

- QHeaderView:
   * [BOBUIBUG-53221] Fixed assert in QHeaderView because of layout changes.
   * [BOBUIBUG-65017] Fixed a font issue when using drag and drop.
   * [BOBUIBUG-56520] Fixed drawing issues in RTL mode.
   * [BOBUIBUG-41124] [BOBUIBUG-54610] Fixed update issues.

- QWidget:
   * [BOBUIBUG-61213] Fixed crash related to the usage of BobUI::WA_WindowPropagation.

- QPlainTextEdit:
   * [BOBUIBUG-62818] Fixed QPlainTextedit hanging because of scrollbar usage.

- QMenu:
   * [BOBUIBUG-63576] Fixed HiDPI issue in QFusionStyle for QCheckBoxes in QMenus.
   * [BOBUIBUG-59794] Fixed HiDPI issue in QMenu when using several screens.

- QMenuItem:
   * [BOBUIBUG-64055] Fixed drawing issue when setting a font style.

- QAbstractButton:
   * [BOBUIBUG-53244] Fixed issue with released signal not being emmited when using
     several mouse buttons at the same time.

- QGraphicsEffect:
   * [BOBUIBUG-60231] Fixed crash when using child widgets with graphic effects.

Third-Party Code
----------------

 - [BOBUIBUG-65138] Fixed glitch in attribution documentation for Freetype
   licenses / BobUI Gui.

Platform Specific Changes
-------------------------

 - eglfs:
   * [BOBUIBUG-65119] Fixed crash when using cursors and multiple GL contexts.

 - iOS/tvOS:
   * BobUI will now take the safe area margins of the device into account when
     computing layouts for BobUIWidgets.

 - macOS:
   * [BOBUIBUG-57487][BOBUIBUG-54160] If you have changed the button mapping of
     your Wacom tablet in System Preferences, it will be respected by BobUI
     applications now. To revert to the old behavior, set the environment
     variable BOBUI_MAC_TABLET_IGNORE_BUTTON_MAPPING.

 - macOS/iOS:
   * [BOBUIBUG-63476] Fixed an issue where text using one of the system theme
     fonts would under certain circumstances display random glyphs.

 - INTEGRITY:
   * Added mkspec for INTEGRITY Qualcomm s820 MSM8996AU

 - QNX:
   * [BOBUIBUG-64033] Fixed a build issue when using slog2 in QNX7.

 - X11 / XCB:
   * [BOBUIBUG-62224] Minimal libXi version requirement has been updated from
     1.7.4 to 1.7.5. This is because XIAllowTouchEvents is known to
     deadlock with libXi 1.7.4 and earlier. When touch events are never
     received, this is not an issue. Plain mouse / keyboard systems are not
     affected.

configure & build system
------------------------

 - [BOBUIBUG-61431][BOBUIBUG-62521] Fixed processing of *_LIBS_{DEBUG|RELEASE}=
   configure command line arguments.
 - [BOBUIBUG-63452] Fixed bogus complaints about DUMMY platform when
   re-configuring a build on a different day than initially configuring it.
 - Fixed logic errors in the conditions of various BobUI features.

qmake
-----

 - [BOBUIBUG-41246][BOBUIBUG-50896][Xcode] It is now possible to enable generation
   of dSYM debug symbols for release builds.
 - [iOS] Fixed compilation of asset catalogs for a generic simulator.
