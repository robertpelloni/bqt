# Copilot Instructions
Please read `AGENTS.md` for core rules.
When generating auto-completions for UI elements, default to `gioui.org/widget/material` implementations for the Go port unless explicitly working inside `BobQUltimatePPHost`.
