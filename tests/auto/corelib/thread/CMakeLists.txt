# Copyright (C) 2022 The BobUI Company Ltd.
# SPDX-License-Identifier: BSD-3-Clause

if(BOBUI_BUILD_WASM_BATCHED_TESTS) # not all tests currently work in WebAssembly
    if(BOBUI_FEATURE_thread)
        add_subdirectory(qatomicint)
        add_subdirectory(qatomicinteger)
        add_subdirectory(qatomicpointer)
        if(BOBUI_FEATURE_future)
            add_subdirectory(qfuturesynchronizer)
            add_subdirectory(qresultstore)
        endif()
        add_subdirectory(qmutexlocker)
        add_subdirectory(qreadlocker)
        add_subdirectory(qwritelocker)
    endif()
    return()
endif()

if(BOBUI_FEATURE_concurrent AND NOT INTEGRITY)
    if(BOBUI_FEATURE_future)
        add_subdirectory(qfuture)
    endif()
endif()

if(BOBUI_FEATURE_thread)
    add_subdirectory(qatomicint)
    add_subdirectory(qatomicinteger)
    add_subdirectory(qatomicpointer)
    add_subdirectory(qatomicwait)
    if(BOBUI_FEATURE_future)
        add_subdirectory(qresultstore)
        add_subdirectory(qfuturesynchronizer)
        if(NOT INTEGRITY)
            add_subdirectory(qpromise)
        endif()
        # BOBUIBUG-87431
        if(TARGET BobUI::Concurrent AND NOT INTEGRITY)
            add_subdirectory(qfuturewatcher)
        endif()
    endif()
    add_subdirectory(qlatch)
    add_subdirectory(qmutex)
    add_subdirectory(qmutexlocker)
    add_subdirectory(qreadlocker)
    add_subdirectory(qreadwritelock)
    add_subdirectory(qsemaphore)
    # BOBUIBUG-85364
    if(NOT CMAKE_CROSSCOMPILING)
        add_subdirectory(bobuihread)
    endif()
    add_subdirectory(bobuihreadonce)
    add_subdirectory(bobuihreadpool)
    add_subdirectory(bobuihreadstorage)
    add_subdirectory(qwaitcondition)
    add_subdirectory(qwritelocker)
endif()

