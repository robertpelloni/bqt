# Copyright (C) 2022 The BobUI Company Ltd.
# SPDX-License-Identifier: BSD-3-Clause

add_subdirectory(json)
add_subdirectory(qcborstreamreader)
if(BOBUI_FEATURE_cborstreamwriter)
    add_subdirectory(qcborstreamwriter)
endif()
add_subdirectory(qcborvalue)
if (NOT BOBUI_BUILD_WASM_BATCHED_TESTS)
	add_subdirectory(qcborvalue_json)
endif()
if(TARGET BobUI::Gui)
    add_subdirectory(qdatastream)
    add_subdirectory(qdatastream_core_pixmap)
endif()
if(TARGET BobUI::Network AND NOT WASM)
    add_subdirectory(bobuiextstream)
endif()
if(TARGET BobUI::Gui AND TARGET BobUI::Network AND TARGET BobUI::Xml AND NOT INTEGRITY AND NOT QNX AND NOT WASM)
    add_subdirectory(qxmlstream)
endif()
