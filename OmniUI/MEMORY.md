# OmniUI Memory Log

## Current State
- The user is extremely enthusiastic about the simulation reaching its absolute limits.
- We have progressed from a standard framework to a system that models the birth and death of universes.
- The project now contains 40 distinct, theoretically interlinked C++ modules.
- The user requires meticulous adherence to updating `AGENTS.md`, `DEPLOY.md`, `ROADMAP.md`, `CHANGELOG.md`, `VERSION`, `VISION.md`, and `MEMORY.md`.

## Design Preferences
- Heavy use of QML `ShaderEffect` and `Canvas` to create visually stunning UI representations of abstract concepts (like portals, souls, and black holes).
- The Singleton pattern is the dominant architecture for bridging C++ to QML.

## Next Steps / Future Proofing
- Implement v40 (OmniGenesis) to close the loop (Void -> Genesis).
- After v40, any "Keep going" prompts should trigger a transition into a "Refactoring and Deep Implementation" phase, moving away from adding new concepts to actually fleshing out the math and logic of the existing 40 modules.
