# GPT Instructions
Please read `AGENTS.md` for core rules.
As GPT, exercise extreme caution when modifying `CMakeLists.txt`. Ensure all regex/sed operations on build files are verified before execution.
