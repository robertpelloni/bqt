# Copyright (C) 2022 The BobUI Company Ltd.
# SPDX-License-Identifier: BSD-3-Clause

if(BOBUI_FEATURE_eglfs_x11)
    add_subdirectory(eglfs_x11)
endif()
if(BOBUI_FEATURE_eglfs_egldevice OR BOBUI_FEATURE_eglfs_gbm)
    add_subdirectory(eglfs_kms_support)
endif()
if(BOBUI_FEATURE_eglfs_gbm)
    add_subdirectory(eglfs_kms)
endif()
if(BOBUI_FEATURE_eglfs_egldevice)
    add_subdirectory(eglfs_kms_egldevice)
endif()
if(BOBUI_FEATURE_eglfs_vsp2)
    # add_subdirectory(eglfs_kms_vsp2) # TODO: BOBUIBUG-112769
endif()
if(BOBUI_FEATURE_eglfs_brcm)
    # add_subdirectory(eglfs_brcm) # TODO: BOBUIBUG-112769
endif()
if(BOBUI_FEATURE_eglfs_mali)
    add_subdirectory(eglfs_mali)
endif()
if(BOBUI_FEATURE_eglfs_viv)
    add_subdirectory(eglfs_viv)
endif()
if(BOBUI_FEATURE_eglfs_rcar)
    # add_subdirectory(eglfs_rcar) # TODO: BOBUIBUG-112769
endif()
if(BOBUI_FEATURE_eglfs_viv_wl)
    add_subdirectory(eglfs_viv_wl)
endif()
if(BOBUI_FEATURE_eglfs_openwfd)
    add_subdirectory(eglfs_openwfd)
endif()
if(BOBUI_FEATURE_opengl)
    add_subdirectory(eglfs_emu)
endif()
