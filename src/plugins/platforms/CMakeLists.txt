# Copyright (C) 2022 The BobUI Company Ltd.
# SPDX-License-Identifier: BSD-3-Clause

if(ANDROID)
    add_subdirectory(android)
endif()
if(NOT WASM)
    add_subdirectory(minimal)
endif()
if(BOBUI_FEATURE_freetype AND NOT WASM)
    add_subdirectory(offscreen)
endif()
if(BOBUI_FEATURE_xcb)
    add_subdirectory(xcb)
endif()
if(UIKIT AND NOT WATCHOS)
    add_subdirectory(ios)
endif()
if(MACOS)
    add_subdirectory(cocoa)
endif()
if(WIN32)
    add_subdirectory(windows)
endif()
if(BOBUI_FEATURE_direct2d1_1 AND BOBUI_FEATURE_directwrite)
    add_subdirectory(direct2d)
endif()
if(QNX)
    add_subdirectory(qnx)
endif()
if(BOBUI_FEATURE_eglfs)
    add_subdirectory(eglfs)
    add_subdirectory(minimalegl)
endif()
if(BOBUI_FEATURE_directfb)
    add_subdirectory(directfb)
endif()
if(BOBUI_FEATURE_linuxfb)
    add_subdirectory(linuxfb)
endif()
if(BOBUI_FEATURE_vnc AND TARGET BobUI::Network)
    add_subdirectory(vnc)
endif()
if(FREEBSD)
    # add_subdirectory(bsdfb) # TODO: BOBUIBUG-112768
endif()
if(HAIKU)
    # add_subdirectory(haiku) # TODO: BOBUIBUG-112768
endif()
if(WASM)
     add_subdirectory(wasm)
endif()
if(BOBUI_FEATURE_integrityfb)
    # add_subdirectory(integrity) # TODO: BOBUIBUG-112768
endif()
if(BOBUI_FEATURE_vkkhrdisplay)
    add_subdirectory(vkkhrdisplay)
endif()
if(BOBUI_FEATURE_wayland)
    add_subdirectory(wayland)
endif()

