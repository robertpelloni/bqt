name: Go WASM CI

on: [push, pull_request]

jobs:
  validate-framework:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Validate BobUI CMake Compatibility
      run: |
        cmake -P cmake/tests/bobui_consolidated_compatibility_validation.cmake -DBOBUI_SKIP_NATIVE_CONFIGURE=ON

  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v4
      with:
        go-version: '1.21'
    - name: Run Go tests
      run: go test ./...
