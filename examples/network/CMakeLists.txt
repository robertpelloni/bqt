# Copyright (C) 2022 The BobUI Company Ltd.
# SPDX-License-Identifier: LicenseRef-BobUI-Commercial OR BSD-3-Clause

if(NOT TARGET BobUI6::Network)
    return()
endif()
if(NOT INTEGRITY AND NOT ANDROID AND NOT WASM)
    bobui_internal_add_example(dnslookup)
endif()
if(TARGET BobUI6::Widgets)
    if(NOT WASM)
        bobui_internal_add_example(blockingfortuneclient)
        bobui_internal_add_example(broadcastreceiver)
        bobui_internal_add_example(broadcastsender)
        bobui_internal_add_example(threadedfortuneserver)
        bobui_internal_add_example(torrent)
        bobui_internal_add_example(multicastreceiver)
        bobui_internal_add_example(multicastsender)
        bobui_internal_add_example(fortuneclient)
        bobui_internal_add_example(fortuneserver)
    endif()
    bobui_internal_add_example(http)
    bobui_internal_add_example(rsslisting)
endif()
if(BOBUI_FEATURE_processenvironment AND TARGET BobUI6::Widgets AND NOT WASM)
    bobui_internal_add_example(network-chat)
endif()
if(BOBUI_FEATURE_ssl AND TARGET BobUI6::Widgets)
    bobui_internal_add_example(securesocketclient)
endif()
if(BOBUI_FEATURE_dtls AND TARGET BobUI6::Widgets)
    bobui_internal_add_example(secureudpserver)
    bobui_internal_add_example(secureudpclient)
endif()
if(BOBUI_FEATURE_sctp AND TARGET BobUI6::Widgets)
    bobui_internal_add_example(multistreamserver)
    bobui_internal_add_example(multistreamclient)
endif()
